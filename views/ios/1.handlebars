{{> nav navIOS=true}}

<div class="container-fluid">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/ios">iOS</a></li>
            <li class="breadcrumb-item active" aria-current="page">1.x</li>
        </ol>
    </nav>
    <div class="row">
        <div class="col-sm-2">
            <div id="toc">
                <div id="tocTitle">Contents</div>
                <ul>
                    <li>1. <a href="#headingFeatures">Features</a></li>
                    <li>2. <a href="#headingHistory">Version History</a></li>
                </ul>
            </div>
        </div>

        <div class="col-sm-10">
            <h1>iPhone OS 1</h1>
            <p>
                iPhone OS 1 was the first major release of <a href="/ios">iOS</a>.
                At the time of release, it was not called "iPhone OS," but instead that it ran a trimmed down version of Mac OS X (now macOS).
                With the release of the <a href="/ios/sdk">iPhone SDK</a> on <span class="date">6 Mar 2008</span>, the name "iPhone OS" was announced.
            </p>

            <h2 id="headingFeatures">Features</h2>
            <p>
                Initially, iPhone OS lacked many features available today;
                There was no wallpaper settings, apps couldn't be rearranged, and there was no <a href="/ios/apps/appStore">App Store</a>.
            </p>

            <h2 id="headingHistory">Version History</h2>
            <table class="table versionHistory">
                <thead>
                    <tr>
                        <th>Version</th>
                        <th>Build</th>
                        <th>Release Date</th>
                        <th>Changelog</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each data}}
                    <tr>
                        <th>{{version}}</th>
                        <td>{{{build}}}</td>
                        <td>{{{date releaseDate}}}</td>
                        <td>
                            {{#if changeNote}}
                            <div class="font-italic">
                                {{{changeNote}}}
                            </div>
                            {{/if}}
                            {{#if changes}}
                            <ul>
                                {{#each changes}}
                                <li>{{{this}}}</li>
                                {{/each}}
                            </ul>
                            {{/if}}
                            {{#if changelogUrl}}
                            <div class="font-italic">
                                <a href="{{changelogUrl}}" class="external">Changelog</a>
                            </div>
                            {{/if}}
                            {{#if securityUrl}}
                            <div class="font-italic">
                                <a href="{{securityUrl}}" class="external">Security Notes</a>
                            </div>
                            {{/if}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>

{{> footer}}